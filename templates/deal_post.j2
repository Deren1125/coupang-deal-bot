{#
  채널 발행 메시지 템플릿 (텔레그램 HTML 서식). 이모지는 글마다 최대 2개 (강조 단계 1개 + 링크 1개).

  변수: product.*, shop.*, discount_rate, avg_price/below_avg_pct, market_price/below_market_pct, link, channels.*
        tier: 'top'(평소 가격 대비 70%↑ 쌈) | 'must'(50%↑) | 'normal',  tier_pct: 그 근거 비율(%)
  필터: won (12,900원), pct (35%), local
  ⚠️ HTML 특수문자는 자동 이스케이프됩니다. <b> <i> <s> <code> <a href> 만 사용 가능.
  ⚠️ 한 줄을 통째로 차지하는 {% if %} / {% endif %} 줄은 출력에서 사라집니다. 줄 "안"의 조건은 {% endif +%} 로 닫아야 줄바꿈이 유지됩니다.
#}
{% if product.headline %}
[{{ product.headline }}]

{% endif %}
{% if tier == "top" %}🚨 {% elif tier == "must" %}🔥 {% endif %}<b>{{ product.name }}</b>
{% if product.has_price %}
{{ product.price | won }}{% if product.original_price %} (정가 {{ product.original_price | won }}){% endif %}{% if discount_rate %} · {{ discount_rate | pct }} 할인{% endif +%}
{% endif %}
{% if product.shipping %}
배송 {{ product.shipping }}
{% endif %}
{% if product.rating or product.review_count %}
{% if product.rating %}별점 {{ product.rating }}{% endif %}{% if product.rating and product.review_count %} · {% endif %}{% if product.review_count %}리뷰 {{ "{:,}".format(product.review_count) }}건{% endif +%}
{% endif %}
{% if tier == "top" %}

역대급 가격입니다. 평소의 3분의 1 수준이라 이 가격은 다시 보기 어려워요. 필요했던 분은 바로 담으세요.
{% elif tier == "must" %}

평소 가격의 절반입니다. 필요했던 분은 꼭 담으세요.
{% elif tier_pct >= 15 %}

평소보다 확실히 싸게 나왔어요.
{% endif %}

👉 {{ link }}
{% if channels.kakao_openchat_url %}

카톡 오픈채팅: {{ channels.kakao_openchat_url }}
{% endif %}
{% if shop.disclosure %}

{{ shop.disclosure }}
{% endif %}
