{#
  스레드(Threads) 발행 템플릿 — 500자 제한, HTML 태그 사용 불가 (순수 텍스트)
  변수는 deal_post.j2 와 동일합니다.
#}
{% if product.headline %}
{{ product.headline }}

{% endif %}
{{ product.name }}
{% if product.has_price %}
💰 {{ product.price | won }}{% if discount_rate %} ({{ discount_rate | pct }} 할인){% endif +%}
{% endif %}
{% if market_price and below_market_pct and below_market_pct > 0 %}
📉 쿠팡 최저가 대비 {{ below_market_pct | pct }} 저렴
{% endif %}
{% if product.rating %}
⭐ {{ product.rating }}{% if product.review_count %} · 리뷰 {{ "{:,}".format(product.review_count) }}건{% endif +%}
{% endif %}

{{ link }}
{% if shop.disclosure %}

{{ shop.disclosure }}
{% endif %}
