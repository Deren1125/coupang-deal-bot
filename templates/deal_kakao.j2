{#
  카카오톡 오픈채팅 복붙용 — 순수 텍스트, 이모지 중심
  카톡은 서식이 없으므로 줄바꿈과 이모지로만 구성합니다.
#}
{% if product.headline %}
[{{ product.headline }}]

{% endif %}
🔥 {{ product.name }}
{% if product.has_price %}
💰 {{ product.price | won }}{% if product.original_price %} (정가 {{ product.original_price | won }}){% endif %}{% if discount_rate %} 🔻{{ discount_rate | pct }}{% endif +%}
{% endif %}
{% if market_price and below_market_pct and below_market_pct > 0 %}
📉 쿠팡 최저가 {{ market_price | won }} 대비 {{ below_market_pct | pct }} 저렴
{% endif %}
{% if avg_price and below_avg_pct and below_avg_pct > 0 %}
📊 최근 30일 평균가 {{ avg_price | won }} 대비 {{ below_avg_pct | pct }} 저렴
{% endif %}
{% if product.rating or product.review_count %}
⭐ {% if product.rating %}별점 {{ product.rating }}{% endif %}{% if product.rating and product.review_count %} · {% endif %}{% if product.review_count %}리뷰 {{ "{:,}".format(product.review_count) }}건{% endif +%}
{% endif %}
{% if product.shipping %}
🚚 {{ product.shipping }}
{% endif %}

👉 {{ link }}
{% if shop.disclosure %}

✱ {{ shop.disclosure }}
{% endif %}
